import Profile from "@/components/Profile";

export const metadata = {
  title: "Profile",
};

const ProfilePage = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default ProfilePage;
